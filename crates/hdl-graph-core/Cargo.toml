[package]
name = "hdl-graph-core"
version = "0.2.0"
edition = "2021"
description = "Core graph data structures and types for HDL Code Graph"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
toml = "0.8"

[lib]
name = "hdl_graph_core"
